# HMAC secret — must match apps/web/.env (REVALIDATE_SECRET)
REVALIDATE_SECRET=replace-with-the-same-secret-as-web

# HMAC secret — must match apps/web/.env (REVALIDATE_SECRET)
REVALIDATE_SECRET=replace-with-the-same-secret-as-web

# URL of the local web dev server (for revalidation after publish)
WEB_URL=http://localhost:3000
